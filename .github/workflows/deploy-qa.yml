name: Deploy QA

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Render QA Deploy
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_QA }}"
